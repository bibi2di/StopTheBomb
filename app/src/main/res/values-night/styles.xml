<?xml version="1.0" encoding="utf-8"?>

<resources>

    <!-- Base application theme for dark mode -->
    <style name="Theme.MyApp" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <!-- Customize dark mode theme here -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryVariant">@color/colorPrimaryVariant</item>
        <item name="colorOnPrimary">@color/colorOnPrimary</item>
        <item name="colorSecondary">@color/colorSecondary</item>
        <item name="colorOnSecondary">@color/colorOnSecondary</item>
        <item name="android:windowBackground">@color/background</item> <!-- Dark background -->
    </style>

    <!-- Customize button styles for dark mode -->
    <style name="ButtonStyle" parent="Widget.MaterialComponents.Button">
        <item name="android:backgroundTint">@color/button_background</item>
        <item name="android:textColor">@color/button_text</item>
    </style>

    <!-- TextView styles -->
    <style name="TextViewStyle" parent="TextAppearance.AppCompat">
        <item name="android:textColor">@color/text</item>
    </style>

    <style name="PreferenceTextStyle">
        <item name="android:textSize">18sp</item> <!-- Increase text size -->
        <item name="android:textColor">@color/text</item> <!-- Optional: set text color -->
    </style>

    <style name="CustomToolbarTheme" parent="ThemeOverlay.AppCompat.Dark.ActionBar">
        <!-- Customize text color -->
        <item name="android:textColorPrimary">@color/white</item>
        <item name="android:textColorSecondary">@color/colorOnSecondary</item>

        <item name="android:background">?attr/colorPrimary</item>

        <item name="titleTextAppearance">@style/CustomToolbarTitle</item>
    </style>

    <style name="CustomToolbarTitle" parent="TextAppearance.AppCompat.Widget.ActionBar.Title">
        <item name="android:textSize">20sp</item>
        <item name="android:textColor">@color/white</item>
        <!--  <item name="android:fontFamily">@font/custom_font</item>  Optional -->
    </style>

</resources>
